{
  "name": "@flow/parser",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build:dev": "yarn run exec build:dev",
    "build:prod": "yarn run exec build:prod",
    "clean": "rm -rf dist",
    "clean:all": "yarn run clean && rm -rf node_modules",
    "test": "yarn run exec test",
    "test:unit": "yarn run exec test:unit",
    "test:watch": "yarn run exec test:watch",
    "lint-staged": "pkg-ok && yarn run exec lint-staged",
    "exec": "FOLDER=parser yarn workspace @flow/build-library"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "lodash": "^4.17.11"
  },
  "dependencies": {
    "is-observable": "^1.1.0",
    "is-promise": "^2.1.0",
    "lodash": "^4.17.11",
    "uuid": "^3.3.2"
  }
}
