import { expect } from 'chai'

describe('demo-test', () => {
  it('1', () => {
    expect(1).to.deep.equal(1)
  })
  it('2', () => {
    expect(1).to.deep.equal(1)
  })
  it('3', () => {
    expect(1).to.deep.equal(1)
  })
})
